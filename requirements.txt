# =============================================================================
# XULCAN - PRODUCTION DEPENDENCIES
# =============================================================================
# Pin all versions for reproducible builds and stability.
# Exclude development tools to minimize attack surface.
# =============================================================================

# === CORE FRAMEWORK & SERVER ===
fastapi==0.109.2
# [PERF] ASGI server; deploy behind Gunicorn in production.
uvicorn[standard]==0.27.1
# [SEC]  Pydantic v2 for strict typing and data validation.
pydantic==2.6.1
pydantic-settings==2.1.0

# === ORCHESTRATION & LOGIC ===
# [DATA] Parse YAML configuration manifests.
PyYAML==6.0.1
# [OPS]  Retry with exponential backoff; implement circuit breaker pattern.
tenacity==8.2.3

# === AI/ML PROVIDERS ===
# Pin closely; provider APIs change frequently.
openai==1.12.0
anthropic==0.18.1
google-genai>=0.5.0
# [COST] Token counting for context window and billing management.
tiktoken==0.6.0
ollama==0.3.0

# === PERSISTENCE ===
# [DATA] Async PostgreSQL driver optimized for high concurrency.
asyncpg==0.29.0
sqlalchemy==2.0.27
# [OPS]  Database schema migrations.
alembic==1.13.1
# [DATA] Redis client for caching and pub/sub messaging.
redis==5.0.1
# [DATA] pgvector extension bindings for vector similarity search.
pgvector==0.2.5

# === SECURITY & AUTHENTICATION ===
# [SEC]  JWT encoding/decoding with cryptographic backend.
python-jose[cryptography]==3.3.0
# [SEC]  Password hashing (Argon2/Bcrypt).
passlib[bcrypt]==1.7.4
# [SEC]  HashiCorp Vault client for secrets management.
hvac==2.1.0

# === CONNECTIVITY ===
# [NET]  Async HTTP client for external API calls and webhooks.
httpx==0.27.0

# === OBSERVABILITY ===
# [OPS]  Structured JSON logging for log aggregation systems.
python-json-logger==2.0.7
# [OPS]  Context-aware structured logging.
structlog==24.1.0

# === UTILITIES ===
python-dotenv==1.0.1
python-multipart==0.0.9

# === XULCAN KERNEL & TEMPLATING ===
# [CORE] Rendering engine for System Prompts and Tool Arguments mapping.
Jinja2==3.1.3

# === EXECUTION & SANDBOXING ===
# [OPS] Docker daemon SDK for ephemeral SandboxExecutor containers.
docker>=7.1.0
requests<2.32.0  # (Opcional, pero ayuda si docker 7.1.0 aún te da guerra)

# === CLI & DEVELOPER EXPERIENCE ===
#[CLI] Command-line interface builder for 'xulcan run' and 'xulcan pull'.
typer==0.9.0
rich==13.7.0

# === STDLIB (STANDARD LIBRARY) ===
#[DATA] HTML parsing for the network_fetch_webpage tool.
beautifulsoup4==4.12.3
lxml==5.1.0

# === CRYPTOGRAPHY ===
# [SEC] Required if using Fernet for Application-Level Encrypted VaultStore.
cryptography==42.0.2