; pytest.ini
; Pytest configuration for the xulcan test suite.

[pytest]
pythonpath = .

; === TEST RUNNER CONFIGURATION ===
; --pspec: BDD-style readable test output (REPLACES -v for better UX).
; -p no:cacheprovider: Completely disables writing the .pytest_cache directory.
; --cov=xulcan: Measure coverage for the 'xulcan' package.
; --cov-report=term: Display the coverage report in the terminal.
addopts =
    --pspec
    -p no:cacheprovider
    --cov=xulcan
    --cov-report=term

; === WARNING FILTERS ===
filterwarnings =
    ignore:The 'app' shortcut is now deprecated.*:DeprecationWarning
    ignore:Pydantic serializer warnings:UserWarning
    ; Add this to silence the 'protected namespace' warnings if you haven't fixed base.py yet
    ignore::UserWarning