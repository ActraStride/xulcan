# Xulcan

Доступные языки: [Английский](../README.md) · [Испанский](README.es.md) · [Французский](README.fr.md) · [Русский](README.ru.md) · [Арабский](README.ar.md) · [Китайский (мандарин)](README.zh.md)

**Xulcan — backend-фреймворк в парадигме API-first для создания, управления и развертывания продвинутых ИИ-агентов.**

Его миссия — абстрагировать сложность оркестрации LLM, управления памятью и использования инструментов, чтобы разработчики могли интегрировать сложные возможности рассуждений в свои приложения с помощью декларативной конфигурации и надежного REST API.

Проект является open source, но создаётся как личная платформа с амбицией вырасти в полноценную экосистему для агентной разработки.

---

## Видение и философия

Xulcan черпает вдохновение в метафоре **циркуля архитектора с универсальным адаптером**. Этот символ отражает наши ключевые принципы:

* **Точность:** Превращение неоднозначного естественного языка в структурированные и точные действия.
* **Модульность:** Формирование агентов из взаимозаменяемых компонентов, таких как инструменты и модули памяти.
* **Оркестрация:** Соединение контекста (фиксированная точка) с действием (подвижная точка) через продуманный процесс рассуждений.

Мы верим в **Configuration as Code** и подход **API-first**, которые обеспечивают развязанные, поддерживаемые и масштабируемые системы ИИ.

---

## Концептуальная архитектура

Xulcan проектируется как управляемый сервис, который взаимодействует с клиентскими приложениями через API.

1. **Панель Xulcan:** Веб-интерфейс, где определяются агенты, инструменты и модули памяти.
2. **Агентное ядро (движок):**

   * **`LLMClient`:** Агностические адаптеры (Gemini, OpenAI, Anthropic).
   * **`ToolExecutor`:** Безопасное выполнение инструментов.
   * **`MemoryManager`:** Краткосрочные (Redis) и долгосрочные (Faiss) хранилища памяти.
   * **`Executor`:** Оркестрация рассуждений (Chain of Thought).
3. **Интеграция клиента:** Простое и безопасное взаимодействие через REST API.

---

## 🛠 Процесс разработки и внесения вкладов

Чтобы сохранять стабильность системы и эффективно управлять релизами, мы следуем **Git Flow**.

### Стратегия веток

* **`main`:** 🔴 **Продакшен.** Только стабильный, версионированный и готовый к развертыванию код. Прямые коммиты запрещены.
* **`develop`:** 🟡 **Интеграция (следующий релиз).** Основная рабочая ветка, куда сливаются и совместно тестируются новые функции.
* **`feature/*`:** 🟢 **Разработка.** Временные ветки для новой функциональности (например, `feature/infra-logging`).

  * Ответвляется от: `develop`
  * Сливается в: `develop`
* **`hotfix/*`:** 🚑 **Срочные исправления.** Критические фиксы продакшена. Ответвляются от `main` и сливаются обратно в `main` и `develop`.

### Конвенция коммитов

Мы придерживаемся стандарта [Conventional Commits](https://www.conventionalcommits.org/), чтобы обеспечивать понятную и семантическую историю изменений:

* `feat:` Новая функциональность.
* `fix:` Исправление ошибки.
* `chore:` Обслуживание или конфигурационные изменения.
* `refactor:` Рефакторинг без изменения поведения.

### Политика Pull Request и слияний

1. **Feature → Develop:**

   * Используем **Squash and Merge**.
   * *Цель:* Каждая функция представлена одним чистым коммитом в истории `develop`.
2. **Develop → Main (Release):**

   * Используем **Merge Commit** (создание merge-коммита).
   * *Цель:* Сохранить хронологию того, что набор функций вышел единой версией (например, v0.1.0).
3. **Тесты:** CI (Docker build + Pytest) должен успешно проходить перед любым слиянием.

---

## Дорожная карта проекта (до мая 2026 года)

### Квартал 1: Фундамент и первый агент

* **[x] Базовая инфраструктура:** Dockerизация, Postgres, Redis и каркас проекта.
* **[ ] Месяц 1:** Проектирование ядра, исследование LLM API, `AgentManager` и первый `LLMAdapter`.
* **[ ] Месяц 2:** Система инструментов (`ToolRegistry`, `ToolExecutor`).
* **[ ] Месяц 3:** Интеграция краткосрочной памяти (Redis) и второй `LLMAdapter`.

### Квартал 2: Продвинутые возможности и экосистема

* **[ ] Месяц 4:** Долгосрочная память (RAG) и третий `LLMAdapter`.
* **[ ] Месяц 5:** Многошаговое рассуждение (Chain of Thought) и воркеры (Celery).
* **[ ] Месяц 6:** MVP панели управления и усиление безопасности и наблюдаемости.

---

## Текущее состояние

🚀 **Фаза активной разработки.**
Базовая инфраструктура (Docker, база данных, кеш) работает.
Идёт внедрение структурированного логирования и средств наблюдаемости.

---

*Этот документ служит путеводной звездой для развития Xulcan. Все технические и продуктовые решения должны соответствовать изложенному здесь видению и архитектуре.*

